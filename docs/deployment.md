# Развёртывание

Для MVP используется Docker Compose: nginx обслуживает SPA и проксирует `/api` в Spring Boot, PostgreSQL хранит сценарии и анонимную статистику. Секреты production-среды должны передаваться через переменные окружения, а доступ к административному API — защищаться отдельно.

